<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <title>{{ page_title }}</title>
    {% if page_description %}
    <meta name="description" content="{{ page_description | escape }}">
    {% endif %}
    {{ content_for_header }}
</head>

<body>

    <h1>{{ page_title }}</h1>

    {{ content_for_layout }}

</body>

</html>